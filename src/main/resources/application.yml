spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/safe_campus
    username: root
    password: 12345678
  servlet:
    multipart:
      max-request-size: 1000MB
      max-file-size: 1000MB

server:
  servlet:
    context-path: /api

# 设置MyBatis-Plus日志输出级别为DEBUG
logging:
  level:
    com.baomidou.mybatisplus: debug